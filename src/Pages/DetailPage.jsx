import useApiRequest from "../components/useApiRequest"
import { Container } from "react-bootstrap"

const DetailPage = (props) => { 

    console.log(props)
    // const detailInfo = useApiRequest(Title)

    return(
        <Container>
            <div>detail Page </div>
        </Container>
    )
}

export default DetailPage